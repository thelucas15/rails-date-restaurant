

<%# link_to "confirm", evaluation_reservations_path(@evaluation),method: :post, class: 'btn btn-primary' %>
<!-- <div>
<h6><%= @restaurant.name %></h6>
<h6><%= @restaurant.address %></h6>
</div>
<div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-4">
        <div class="card">
          <h6><%= @restaurant.name %></h6>
          <h6><%= @restaurant.address %></h6>
        </div>
      </div>
    </div>
  </div> -->
<div class="restaurant-card text-center">

  <h2>Congratulations!</h2>
  <div>
  <h3><%= @evaluation.selector.username %></h3>
  </div>

  <p>Your're going to the restaurant:</p>
  <h4 class="restaurant-name"> <%= @restaurant.name %></h4>
  <p>on <%= @evaluation.match_list.date.strftime("%d %b, %Y") %> at <%= @evaluation.match_list.start_time.strftime("%H:%M") %></p>
  <p>with:</p>
  <h3><%= @evaluation.selectee.username %></h3>
  <hr>
  <p><strong>Restaurant address:</strong></p>
  <p><%=  @restaurant.address %></p>

          <div class="div-spacer"></div>
    <%= simple_form_for([@evaluation, @reservation]) do |f| %>
    <%= f.input :restaurant_id , as: :hidden %>
    <%= f.input :evaluation_id , as: :hidden %>
    <%= f.button :submit, 'Confirm', mehotd: :post, class: 'btn btn-primary choose-btn' %>
    <% end %>
    </div>
</div>



<!-- (date: @date, start_time: @start_time,  food_type: @restaurant.food_type), class: 'card-link'
 --><div class="back">

    <%= link_to "back", restaurants_path %>
</div>

  <!-- <div class="no-invitations">
    <div class="no-invitations-message text-center">
      <p>Hey buddie, you have no pending invitations.</p>
    </div>
  </div> -->
